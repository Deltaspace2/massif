# The FastAPI backend. Fetched server-side, so no CORS setup is needed.
# 127.0.0.1 rather than localhost: Node resolves localhost to ::1 first,
# and uvicorn binds 127.0.0.1 only, which fails as "fetch failed".
MASSIF_API=http://127.0.0.1:8000
